<%- include('partials/header') %>
<h1><%= title %></h1>
<form method="POST">
  <div class="form-group">
    <label for="message_title">Title</label>
    <input id="message_title" name="message_title" type="text" class="form-control" value="<%= typeof message_title !== 'undefined' ? message_title : '' %>" placeholder="Title"/>
  </div>
  <div class="form-group">
    <label for="message_text">Message Text</label>
    <input id="message_text" name="message_text" type="text" class="form-control" value="<%= typeof message_text !== 'undefined' ? message_text : '' %>" placeholder="Message text"/>
  </div>
  <button class="btn btn-primary" type="submit">Submit</button>
</form>

<% if (errors && errors.length > 0) { %>
  <ul>
    <% errors.forEach(function(error) { %>
      <li><%= error.msg %></li>
    <% }) %>
  </ul>
<% } %>

<%- include('partials/footer') %>
